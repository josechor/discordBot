services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: meliodas
    volumes:
      - .:/app # Monta el código local en el contenedor
      - ./data:/app/data
    restart: always
    command: ["npm", "run", "test"] # Ejecuta el script de nodemon
